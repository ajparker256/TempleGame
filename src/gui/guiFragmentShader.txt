#version 140

in vec2 textureCoords;

out vec4 out_Color;

uniform sampler2D guiTexture;

void main(void){

	vec4 colorshift = vec4(0,1.0,1.0,1.0);	
	out_Color = colorshift*texture(guiTexture,textureCoords);

}